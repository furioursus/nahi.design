---
import type { ImageMetadata } from "astro";

interface Props {
    image: ImageMetadata;
    alt: string;
    breakout?: boolean;
    transitionName?: string;
}

const { image, alt, breakout = false, transitionName } = Astro.props;
---
